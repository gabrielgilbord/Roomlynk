# ⚠️ DEPRECADO — NO EJECUTAR

Este archivo crea tablas en `public` y **colisiona** con otras apps del mismo proyecto Supabase.

Usa en su lugar:
- `002_roomlynk_schema.sql` (schema aislado `roomlynk`)
- `003_invitation_rpc.sql` (RPC invitaciones + reparto gastos)
